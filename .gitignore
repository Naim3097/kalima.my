node_modules
.next
out
build
*.local
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
.claude/settings.local.json

# Supabase CLI scratch
supabase/.temp
.env*.local

# Product videos — 30MB of .mp4 the storefront never loads. The photography
# beside them is versioned; the clips stay local (upload to Storage if needed).
public/products/**/*.mp4
